---
---

<div class="finale-upper-div">
    <div class="finale-div">
        <div class="lines-top">
            <div class="finale-underline-top-3" />
            <div class="finale-underline-top-2" />
            <div class="finale-underline-top-1" />
        </div>

        <h1 class="finale-text">SEIZOENSFINALE!</h1>

        <div class="lines-bot">
            <div class="finale-underline-bot-1" />
            <div class="finale-underline-bot-2" />
            <div class="finale-underline-bot-3" />
        </div>
    </div>

    <div class="data-container">
        <h1 class="data-header">20, 21, 22 ÉN 23 APRIL!</h1>
    </div>

    <div class="info-containers">
        <div class="container-left box-container">
            <div class="stagger-back back-1"></div>
            <div class="stagger-back back-2"></div>
            <div class="stagger-back back-3"></div>
            <div class="main-box">
                <h2 class="box-header">WAAR, WANNEER</h2>
                <p class="box-content">20, 21, 22 ÉN 23 April</p>
                <p class="box-content">DE KOELISSE - STEENGROEVELAAN 3, 3001 LEUVEN</p>
            </div>
        </div>

        <a href="https://widget.weezevent.com/ticket/E1852011/?code=63368&locale=nl-NL&width_auto=1&color_primary=0032FA"
            target="_blank"
            class="container-right box-container ticket-container">
            <div class="stagger-back back-1"></div>
            <div class="stagger-back back-2"></div>
            <div class="stagger-back back-3"></div>
            <div class="main-box">
                <h2 class="box-header">TICKETS</h2>
                <p class="box-content">KLIK HIER OM JE TICKETS TE KOPEN</p>
                <p class="box-content">OP = OP</p>
            </div>
        </a>
    </div>
</div>

<style>
    .finale-upper-div {
        background-color: transparent;
        height: 100%;
    }

    .finale-div {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;

        gap: 0px;

        position: relative;
        top: 6em;
    }

    .finale-text {
        color: #D3B054;

        margin: 0;

        font-size: clamp(2rem, 10vw, 8rem);
        font-family: 'Tilt Warp', sans-serif;

        filter:
            brightness(1.05)  
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .lines-top, .lines-bot {
        display: flex;
        flex-direction: column;
        gap: clamp(15px, 0.5vw, 15px);
        width: 100%;
    }

    .lines-top {
        margin-bottom: 0.5em;
    }

    .lines-bot {
        margin-top: 0.5em;
    }

    .finale-underline-top-3 { 
        width: 35%; 
        margin-right: auto; 
        background-color: rgba(208, 143, 70, 0.9); 
        
        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .finale-underline-top-2 { 
        width: 50%; 
        margin-right: auto; 
        background-color: rgba(210, 160, 76, 0.95);  
        
        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4));   
    }

    .finale-underline-top-1 { 
        width: 65%; 
        margin-right: auto; 
        background-color: rgba(211, 176, 84, 1);  
        
        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .finale-underline-bot-1 { 
        width: 65%; 
        margin-left: auto; 
        background-color: rgba(211, 176, 84, 1);  

        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .finale-underline-bot-2 { 
        width: 50%; 
        margin-left: auto; 
        background-color: rgba(210, 160, 76, 0.95);  

        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .finale-underline-bot-3 { 
        width: 35%; 
        margin-left: auto; 
        background-color: rgba(208, 143, 70, 0.9);  

        filter: 
            brightness(1.05)
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    .lines-top div, .lines-bot div {
        height: 1em;
    }

    .data-container {
        display: flex;
        
        margin-top: 8em;
        margin-bottom: 8em;

        padding: 1em;

        justify-content: center;
        align-items: center;
    }

    .data-header {
        color: #D3B054;

        margin: 0;

        font-size: clamp(2rem, 5vw, 8rem);
        font-family: 'Tilt Warp', sans-serif;

        filter:
            brightness(1.05)  
            drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); 
    }

    /* Flex container: horizontal on desktop, vertical on mobile */
    .info-containers {
        display: flex;
        gap: 5em;
        margin-top: -6em;
        align-items: top;
        justify-content: center;
        flex-wrap: wrap;
    }

    /* Each box container relative for staggered backgrounds */
    .box-container {
        position: relative;
        flex: 1 1 300px; /* flexible, minimum width 300px */
        min-width: 350px;
        max-width: 550px;
    }

    /* Main visible box */
    .main-box {
        position: relative;
        background-color: rgba(211, 176, 84, 1); /* matching top/bot line color */
        padding: 2em;
        z-index: 10;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.2);

        text-align: center; /* centers headers and paragraphs */
    }

    /* Box header styling */
    .box-header {
        margin-top: 0;
        color: white;
        font-family: 'Tilt Warp', sans-serif;
        font-size: 2rem;
        filter: 
            brightness(1.05) 
            drop-shadow(0 0 5px rgba(255,255,255,0.3));
    }

    .box-small-header {
        margin-top: 0;
        margin-bottom: 1em;
        color: white;
        font-family: 'Tilt Warp', sans-serif;
        font-size: 1.4rem;
        filter: 
            brightness(1.05) 
            drop-shadow(0 0 5px rgba(255,255,255,0.2));
    }

    .box-content {
        color: white;
        font-family: 'Tilt Warp', sans-serif;
    }

    .ticket-container {
        text-decoration: none;
        color: inherit;
        display: block;
    }

    /* Staggered background boxes */
    .stagger-back {
        position: absolute;
        border-radius: 10px;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
    }

    .back-1 { background-color: rgba(211, 176, 84, 0.6); transform: translate(5px, 5px); }
    .back-2 { background-color: rgba(210, 160, 76, 0.45); transform: translate(10px, 10px); }
    .back-3 { background-color: rgba(208, 143, 70, 0.3); transform: translate(15px, 15px); }

    /* Responsive adjustments */
    @media (max-width: 1000px) {
        .info-containers {
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }

        .box-container {
            min-width: 420px;
            max-width: 420px;
        }

        .stagger-back {
            height: 68%
        }

        .back-1 { transform: translate(2px,2px); height: 68%; }
        .back-2 { transform: translate(4px,4px); height: 68%;}
        .back-3 { transform: translate(6px,6px); height: 68%;}
    }

    .header-ticket-link,
    .header-ticket-link:visited,
    .header-ticket-link:hover,
    .header-ticket-link:active {
        color: white;
        text-decoration: underline;
        display: inline-block;
        
        transition: all 75ms ease-out;

        filter: 
            brightness(1.1);
    }

    .header-ticket-link:hover {
        transform: translateY(-3px);
        
        filter:
            drop-shadow(0 0 10px rgba(255, 255, 255, 0.12)); 
    }

    .container-right,
    .container-right:visited,
    .container-right:hover,
    .container-right:active {
        transition: all 75ms ease-out;
    }

    .container-right:hover {
        transform: translateY(-3px);
    }
</style>